# Project-local registry override.
# Without this, npm would fall back to ~/.npmrc which on some dev machines
# (e.g. Apollo) points to a private corporate registry that CI cannot reach.
registry=https://registry.npmjs.org/
